./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) append)'
>>>
<procedure>
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) nil)'
>>>
nil
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) (append (quote (a)) (quote (b c d))))'
>>>
(a b c d)
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) (append (quote (1)) (quote (4 5 6))))'
>>>
(1 4 5 6)
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) (append nil (quote (1))))'
>>>
(1)
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) (append (quote (1)) nil))'
>>>
(1)
>>>= 0

./runlisp.py '(progn (define (append a b) (if (null? a) b (cons (car a) (append (cdr a) b)))) (append nil nil))'
>>>
nil
>>>= 0
